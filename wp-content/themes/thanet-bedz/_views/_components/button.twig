<a href="{{ link }}" {{ target ? 'target="' ~ target ~ '"' }} class="btn btn-{{ colour|default('primary') }} {{ classes }}" {{ tracking ? 'onClick="' ~ tracking ~ '"' }}>{{ text }}{{ fn('icon','caret-right') }}</a>